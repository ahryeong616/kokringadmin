# 현장 시세조사 — 인터넷 주소로 어디서나 (Railway + GitHub)

주소 하나만 있으면 매장에서 폰으로, 집에서 PC로 **같은 기록**을 봅니다.
콕링 재고관리와 같은 방식(같은 서버·같은 저장소)이라, 이미 그걸 올려 두셨다면 새로 만들 게 거의 없습니다.

- 주소: `https://내주소.up.railway.app/survey`
- 기록은 **기기에 먼저 저장**되므로 창고에서 신호가 없어도 조사가 끊기지 않습니다. 연결되면 알아서 올라갑니다.

---

## 1단계. GitHub에 올리기

이 저장소를 GitHub에 올립니다(이미 `ahryeong616/kokringadmin` 에 있습니다).
Railway는 GitHub에 올라간 내용을 그대로 가져가 실행합니다.

## 2단계. Railway 프로젝트 만들기

1. https://railway.app 접속 → **GitHub 계정으로 로그인**
2. **New Project** → **Deploy from GitHub repo** → `kokringadmin` 선택
3. 자동으로 빌드가 시작됩니다. (아직 DB가 없어 실패해도 정상입니다 — 3단계에서 붙입니다)

## 3단계. 데이터베이스 붙이기

> **반드시 앱과 같은 프로젝트 안에** 만드세요. 프로젝트가 다르면 서로 연결되지 않습니다.
> 화면 왼쪽 위의 프로젝트 이름이 앱이 있는 프로젝트와 같은지 확인하세요.

1. 앱 서비스가 있는 캔버스의 빈 곳에서 **우클릭** (또는 **+ Create**) → **Database** → **Add MySQL**
2. MySQL 카드가 `Online` 이 되면 끝입니다.

MySQL 화면에 **You have no tables** 라고 나와도 정상입니다.
표는 서버가 처음 켜질 때 알아서 만듭니다. **Create table 은 누르지 마세요.**

## 4단계. 앱에 변수 두 개 넣기

Railway 는 서비스마다 변수가 따로 놉니다. MySQL 이 가진 접속 주소를 **앱 서비스로 연결**해 줘야 합니다.

앱 서비스 카드(`kokring-inventory`) → **Variables** → **New Variable** 로 두 개를 넣습니다.

| 이름 | 값 |
|---|---|
| `DATABASE_URL` | `${{MySQL.MYSQL_PRIVATE_URL}}` |
| `APP_ACCESS_KEY` | `kok-fWqWfF0p2vemtVrR8s5cgCAu` |

`DATABASE_URL` 값은 직접 타는 대신, 값 칸에 `${{` 를 치면 **다른 서비스 변수 목록**이 뜹니다.
거기서 `MySQL` → `MYSQL_PRIVATE_URL` 을 고르면 됩니다.

> 목록에 `MYSQL_PRIVATE_URL` 이 없으면 `MYSQL_URL` 을 고르세요. 둘 다 동작합니다.
> private 쪽이 같은 프로젝트 안에서 더 빠르고 요금도 들지 않습니다.
> 정확한 이름은 **MySQL 카드 → Variables** 탭에서 확인할 수 있습니다.

`APP_ACCESS_KEY` 는 **앱에 들어갈 때 쓰는 비밀번호**입니다. 위 값을 그대로 쓰셔도 되고
직접 정하셔도 됩니다. 폰과 PC에 같은 값을 넣어야 기록이 서로 맞춰집니다.
아는 사람은 누구나 기록을 보고 고칠 수 있으니 남에게 알려주지 마세요.

`PORT` 는 Railway 가 알아서 넣어 줍니다.

### 예전에 쓰던 변수가 남아 있다면

Aiven·Render 로 쓰던 `DB_SSL`, `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `DATABASE_URL` 이
Variables 에 남아 있으면 **지우세요.** Railway 안의 MySQL 은 내부망으로 연결되어 SSL 이 필요 없고,
남은 옛 주소가 우선 적용되면 엉뚱한 DB 로 붙습니다. `APP_ACCESS_KEY` 만 남기면 됩니다.

## 5단계. 주소 만들기

웹 서비스 카드 → **Settings** → **Networking** → **Generate Domain**
→ `https://무엇무엇.up.railway.app` 주소가 생깁니다.

## 6단계. 폰·PC에서 켜기

1. 브라우저에서 **`주소/survey`** 를 엽니다.
2. **내보내기** 탭 → **기기 간 동기화 (서버)** 에 4단계의 `APP_ACCESS_KEY` 를 넣고 **동기화 켜기**
3. 맨 위에 초록 점과 함께 `동기화됨` 이 뜨면 됩니다.
4. 다른 기기에서도 같은 주소·같은 비밀번호로 똑같이 하면 기록이 서로 맞춰집니다.

폰에서는 브라우저 메뉴의 **홈 화면에 추가**를 하면 앱처럼 열립니다.

---

## 어떻게 맞춰지나

- 저장·수정·삭제할 때마다 **기기에 먼저** 쓰고, 그 다음 서버로 올립니다.
- 다른 기기가 바꾸면 서버가 곧바로 알려 줍니다(화면이 저절로 갱신됩니다).
- 인터넷이 끊기면 기기에만 쌓아 두었다가, 연결되는 순간 밀어 올립니다.
- 같은 기록을 두 기기에서 고쳤다면 **나중에 고친 쪽**이 남습니다.
- **사진·동영상은 기기에만 남습니다.** 용량이 커서 서버로 보내지 않습니다.
  다른 기기로 옮기려면 내보내기 탭의 **미디어 저장**을 쓰세요.

## 잘 안 될 때

| 증상 | 확인할 것 |
|---|---|
| `접속 비밀번호가 맞지 않습니다` | 4단계의 `APP_ACCESS_KEY` 와 앱에 넣은 값이 같은지 |
| 계속 `오프라인` | 인터넷 연결. 주소가 `https://` 로 시작하는지 |
| 배포가 실패함 | Railway **Deployments** → 로그에 `Missing environment variables: APP_ACCESS_KEY` 가 보이면 4단계를 다시 |
| `ECONNREFUSED` 로그 | MySQL 이 앱과 **같은 프로젝트** 안에 있는지, `DATABASE_URL` 참조를 넣었는지 |
| `getaddrinfo ENOTFOUND` | `DATABASE_URL` 이 `${{MySQL.…}}` 참조가 아니라 글자로 들어간 경우 |
| 기록이 안 넘어옴 | 두 기기 모두 동기화가 **켜져** 있는지, 같은 주소인지 |

## 비용

Railway 무료 크레딧으로 시작할 수 있고, 소규모 사용에서는 월 몇 달러 수준입니다.
정확한 요금은 Railway 요금 안내를 확인하세요.
