# 현장 시세조사 — 사진 자동입력 사용법

`현장_시세조사.html` 파일 하나만 있으면 됩니다. 더블클릭해서 브라우저로 열면 바로 씁니다.
(설치·서버·인터넷 연결 없이도 표 입력과 계산은 동작합니다. 사진 인식만 인터넷이 필요합니다.)

## 사진으로 자동입력하는 법

1. 위쪽 **조사 매장**을 고릅니다.
2. **인식 엔진**을 고릅니다.
   - **무료 인식 (기본값)** — 돈이 들지 않고 장수 제한이 없습니다. 사진 한 장당 상품 하나 기준입니다.
   - **AI 정밀인식 (유료)** — Claude API 키가 필요합니다. 훨씬 정확하고, 진열대 사진 한 장에서
     여러 상품을 한꺼번에 뽑아냅니다. 사진당 약 9~45원(모델에 따라 다름).
3. **사진 촬영**(폰) 또는 **앨범/파일 선택**을 누릅니다.
   드래그앤드롭, 클립보드 붙여넣기(Ctrl+V)도 됩니다. **여러 장을 한꺼번에** 올릴 수 있습니다.
4. 아래에 **인식 결과 확인** 카드가 뜹니다. 잘못 읽은 칸만 고치고 **표에 추가**를 누릅니다.

자동으로 채워지는 항목: 상품명 · 판매가(매입가) · 정가 · 상품코드 · 창고 위치코드 ·
박스입수 · 부가세 별도 여부 · 분류(상품명으로 추정)

### AI 정밀인식 키 만들기
1. https://console.anthropic.com 에서 로그인 → API Keys → Create Key
2. `sk-ant-...` 로 시작하는 키를 복사해 앱의 **Claude API 키** 칸에 붙여넣습니다.
3. 키는 이 브라우저(localStorage)에만 저장되고 다른 곳으로 전송되지 않습니다.
   백업 JSON에도 키는 들어가지 않습니다. 공용 PC에서는 쓰지 마세요.

### 무료 인식의 정확도 (실측)

컴퓨터로 만든 깨끗한 가격표로 측정한 결과입니다.

| 항목 | 결과 |
|---|---|
| 판매가 | 잘 읽음 |
| 상품코드 | 잘 읽음 |
| 창고 위치코드 | 잘 읽음 |
| 박스입수 | 잘 읽음 |
| 상품명 | **몇 글자씩 틀림** (예: `ABM도톰한…` → `^468도톰한…`) |

숫자는 잘 읽고 한글 상품명에서 자주 틀립니다. 실제 사진은 이보다 떨어지니,
**인식 결과 확인** 칸에서 상품명만 훑어 고치는 방식이 가장 빠릅니다.

처리 속도는 사진 한 장당 약 2초이고, CPU 코어 수만큼(최대 3장) 동시에 처리합니다.
사진이 아주 많으면 **확인 없이 바로 표에 추가**를 켜고 나중에 표에서 한꺼번에 고치세요.

### 사진을 잘 찍는 요령
- 가격표가 화면을 꽉 채우도록 **정면에서** 찍기
- 그림자·반사 피하기, 밝은 곳에서 찍기
- 진열대 전체 사진은 AI 정밀인식에서만 여러 건으로 분리됩니다

## 오프라인판 만들기 (인터넷 없이 쓰기)

저장소의 `현장_시세조사.html` 은 인식 엔진을 인터넷에서 받아옵니다.
엔진과 한글 데이터까지 파일 안에 넣어 **인터넷 없이** 쓰려면:

```bash
mkdir vendor && cd vendor
npm pack tesseract.js@5.1.0 tesseract.js-core@5.1.1
tar xzf tesseract.js-5.1.0.tgz && tar xzf tesseract.js-core-5.1.1.tgz --one-top-level=core
cp package/dist/tesseract.min.js package/dist/worker.min.js .
cp core/package/tesseract-core-simd-lstm.wasm.js .
curl -LO https://raw.githubusercontent.com/naptha/tessdata/gh-pages/4.0.0_fast/kor.traineddata.gz
curl -LO https://raw.githubusercontent.com/naptha/tessdata/gh-pages/4.0.0_fast/eng.traineddata.gz
cd .. && node build-offline.mjs
```

`현장_시세조사_오프라인.html`(약 8MB)이 만들어집니다. 이 파일 하나만 있으면 비행기 모드에서도 동작합니다.

## 계산 방식

엑셀 `마진계산` · `수수료표` 시트의 식을 그대로 옮겼습니다.

```
실매입 단가   = 매입가 × (부가세 별도면 1.1) ÷ 포장수량
건당 매입원가 = 실매입 단가 × 판매 묶음수량
물류비        = 기본 택배비 + 포장 부자재비
총원가        = 건당 매입원가 + 판매수수료 + 결제수수료 + 수수료 부가세 + 물류비
건당 마진     = 쿠팡 판매가 − 총원가
손익분기 판매가 = (건당 매입원가 + 물류비) ÷ (1 − (판매수수료율 + 결제수수료율) × 1.1)
목표마진 판매가 = (건당 매입원가 + 물류비) ÷ (1 − 목표마진율 − (판매수수료율 + 결제수수료율) × 1.1)
```

판매수수료율은 분류에 따라 자동 적용됩니다(생활용품·주방 7.8% / 문구·사무 7.9% /
뷰티 9.6% / 완구·자동차 10% / 패션잡화 10.5% / 가공식품 10.6% / 신선식품 10.9% / 가전·디지털 4.8%).
그 밖의 기준값은 아래 **판매비용 기준값**에서 바꿀 수 있습니다.

## 매장별 가격 비교

같은 상품을 어느 매장이 제일 싸게 파는지 나란히 놓고 봅니다.

- **비교 기준** — 기본값은 **실매입 단가**입니다. 부가세 별도 매장과 포함 매장을 공정하게 비교하려면
  이 기준이어야 합니다. (예: 표시가 2,900원 부가세 별도 = 실제 3,190원)
  포장수량이 여러 개인 박스 상품도 낱개 단가로 환산합니다.
- **최저가 셀은 초록색**, 최고가는 빨간 글씨로 표시됩니다.
- 위쪽 매장 카드에 **매장별 최저가 획득 횟수**가 나옵니다. 어느 매장이 전반적으로 싼지 한눈에 보입니다.
- **차액·절감률**이 큰 상품이 위로 옵니다. 바꿔서 살 때 이득이 가장 큰 순서입니다.

### 같은 상품을 어떻게 알아보나

상품명은 매장마다 다르게 적히고 무료 인식은 글자를 흘리기 때문에, 이름이 똑같아야만 묶이면 쓸 수 없습니다.
그래서 세 가지로 판단합니다.

1. **비교이름이 같으면** 무조건 함께 묶습니다 (사람이 정한 것이 최우선)
2. **상품코드가 같으면** 이름이 달라도 같은 상품입니다
3. **이름 유사도**가 기준 이상이면 묶습니다

단, **용량·수량·사이즈가 어긋나면 절대 묶지 않습니다.** 이게 없으면 `압축분무기 500ml` 와 `1L` 가
서로 묶여 버립니다(이름 유사도 0.74로, OCR이 깨뜨린 같은 상품 0.67보다 오히려 높습니다).
`6개입`과 `12개입`, 비치백 `중`과 `소`도 같은 이유로 갈라집니다.

### 잘못 묶였을 때

상품명 옆 **✎** 를 누르면 그 묶음에 들어간 항목들이 다 보이고, **비교이름**을 정할 수 있습니다.
서로 다른 매장의 상품에 같은 비교이름을 적으면 항상 함께 비교되고, 다른 이름을 적으면 갈라집니다.
자동으로 잘 안 되면 묶는 기준을 **느슨하게 / 엄격하게** 로 바꿔 보세요.

## 저장과 내보내기

- 입력한 내용은 브라우저에 자동 저장됩니다(사진 미리보기 포함).
- **가격조사 CSV** — 엑셀 `가격조사` 시트와 같은 열 구성
- **마진계산 CSV** — 엑셀 `마진계산` 시트와 같은 열 구성
- **비교표 CSV** — 매장이 열, 상품이 행인 가격 비교표
- **백업(JSON)** — 다른 PC·폰으로 옮길 때. 받는 쪽에서 **복원**을 누릅니다.
- 저장 공간이 부족해지면 사진 미리보기만 자동으로 비우고 데이터는 지킵니다.
