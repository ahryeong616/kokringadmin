# 콕링 재고관리 - 어디서나 접속하기 (무료 배포)

인터넷 주소만 있으면 노트북·휴대폰 어디서나 접속할 수 있게 만드는 방법입니다.
내 PC를 켜둘 필요가 없습니다.

구성:
- 데이터베이스: **Aiven** 무료 MySQL (데이터 영구 보관, 카드 불필요)
- 프로그램(웹 서버): **Render** 무료 (GitHub 저장소와 연결)

> 참고: Render 무료 플랜은 15분 동안 아무도 접속하지 않으면 잠자기 상태가 되고,
> 그 뒤 첫 접속 때 30~60초 정도 느리게 열립니다. 접속되고 나면 정상 속도입니다.

---

## 1단계. Aiven에서 무료 MySQL 만들기

1. https://aiven.io 접속 → **Get started for free** 로 가입 (구글 계정 가능, 카드 불필요)
2. 로그인 후 **Create service**(서비스 만들기) 클릭
3. **MySQL** 선택
4. 요금제에서 **Free**(무료) 플랜 선택
5. 지역(Cloud/Region)은 아무거나(가까운 곳) → **Create service** 클릭
6. 서비스가 "Running"(초록불)이 될 때까지 1~2분 기다립니다.
7. 서비스 화면의 **Connection information**(연결 정보)에서 **Service URI** 를 복사합니다.
   - 이렇게 생겼습니다:
     `mysql://avnadmin:AVNS_xxxxxxxx@mysql-xxxx.aivencloud.com:12345/defaultdb?ssl-mode=REQUIRED`
   - 이 한 줄을 잘 보관하세요. (다음 단계에서 씁니다)

---

## 2단계. Render에 프로그램 올리기

1. https://render.com 접속 → **GitHub 계정으로 가입/로그인**
2. 오른쪽 위 **New +** → **Blueprint** 클릭
3. 이 저장소(`kokringadmin`)를 선택 → **Connect**
   - 저장소에 있는 `render.yaml` 을 자동으로 읽어 설정을 채워 줍니다.
4. 배포 중에 **두 가지 비밀값**을 입력하라고 나옵니다:
   - **DATABASE_URL** : 1단계에서 복사한 Aiven **Service URI** 를 그대로 붙여넣기
   - **APP_ACCESS_KEY** : 접속용 비밀번호. 아래처럼 길고 복잡하게 정하세요.
     - 예: `Ys1CoHcaWTTjw7mb` (그대로 쓰지 말고 본인만의 값으로 바꾸세요)
5. **Apply / Deploy** 클릭 → 빌드가 끝날 때까지 2~4분 기다립니다.
6. 완료되면 화면 위에 주소가 생깁니다:
   `https://kokring-inventory.onrender.com` (이름은 다를 수 있음)

---

## 3단계. 접속하기

- 위 주소를 노트북·휴대폰 브라우저 주소창에 입력
- 비밀번호에 2단계에서 정한 **APP_ACCESS_KEY** 입력
- 같은 주소·같은 비밀번호로 어느 기기에서든 같은 재고 데이터가 보입니다.

휴대폰 홈 화면에 추가해두면 앱처럼 쓸 수 있습니다.

---

## 자주 겪는 문제

- **첫 접속이 느려요 (30초~1분)**: Render 무료 플랜이 잠자기에서 깨어나는 중입니다. 정상입니다.
- **로그인/화면이 안 떠요**: Render 서비스 화면의 **Logs**(로그) 탭에서 빨간 오류를 확인하세요.
  대부분 DATABASE_URL 을 잘못 붙여넣었거나, Aiven 서비스가 아직 Running이 아닌 경우입니다.
- **비밀번호를 바꾸고 싶어요**: Render 서비스 → **Environment** 탭 → `APP_ACCESS_KEY` 값 수정 → 저장(자동 재배포).

---

## 로컬(내 PC)에서 실행하는 방법은 그대로입니다

클라우드 배포와 상관없이, 내 PC에서 쓰던 방식도 계속 동작합니다:

```
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
cd D:\kokringadmin-main\kokringadmin-main
node server.js
```

→ 브라우저 `http://localhost:3000`
